using UnityEngine;
using System.Collections;

public class Ground : MonoBehaviour {
	public double heightLevel; // A value of 0.0 - 10.0 (for now) in increment of 0.5 to use a standard for the camera
}
